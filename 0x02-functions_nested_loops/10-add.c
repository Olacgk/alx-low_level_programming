#include "main.h"

/**
 * add - add two integers
 * @first: the first integer
 * @sec: the second integer
 * Return: the sum of the both integers
 */
int add(int first, int sec)
{
	int sum;

	sum = first + sec;

	return (sum);
}
